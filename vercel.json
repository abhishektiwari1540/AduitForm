{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build"
    }
  ],
  "installCommand": "rm -rf node_modules package-lock.json && npm install --legacy-peer-deps --no-audit",
  "buildCommand": "npm run build",
  "outputDirectory": "build"
}